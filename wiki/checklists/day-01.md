# Чеклист: день 1. Твои руки, не мой автопилот

Код-заготовку я накидал, чтобы было что ломать. **Курить React = ты сам трогаешь файлы.** Пока пункты не закрыты — день 1 не сдан.

Старт: `cd 01-react && pnpm install && pnpm dev`

Гайд с объяснениями синтаксиса: [migrate/day-01.md](../migrate/day-01.md).  
Формат кода: [tools/format.md](../tools/format.md) → `pnpm format`.

---

## Обязательно понять (отметь сам)

- [ ] Нашёл `main.tsx` и понял: это `createApp().mount`, только `createRoot().render`
- [ ] Понял, зачем `StrictMode` (dev-проверка, не прод-магия)
- [ ] Понял, почему `return (` в скобках на несколько строк JSX
- [ ] Понял: `onClick={() => fn(id)}` ≠ `onClick={fn(id)}`
- [ ] Понял: `onAddToList={(id) => …}` — это emit через колбэк-проп, не «странная поебота React»
- [ ] Понял: `useState` + `setX`, объект/массив только новой копией
- [ ] Понял: `value` + `onChange` = v-model

---

## Сделай руками в коде (без меня)

1. [ ] В `mockProducts.ts` добавь 5-ю позицию своим текстом. Увидел в сетке.
2. [ ] В `ProductCard` поменяй текст кнопки на свой. Клик всё ещё добавляет в «В списке».
3. [ ] Сломай специально: в `App` вместо `setProducts([...])` напиши `products.push(...)` и кликни «Добавить». Экран не обновится. Верни через setter. Это главный урок дня.
4. [ ] Вынеси колбэк добавления в список в отдельную функцию в `App` (не стрелка внутри JSX) — чтобы видеть, что `() =>` не обязательна, если сигнатура совпадает / обернул сам.
5. [ ] В `SearchField` добавь кнопку «Сбросить» которая зовёт `onChange('')` — колбэк вверх без emit API.
6. [ ] Прогони `pnpm format` после правок. Потом `pnpm lint` — если орнёт на `if (!x)`, поправь на явный `!==`.

---

## Когда писать мне

Не «сделай за меня день 1». Пиши:

- «сломал вот так — экран мёртвый, вот дифф»  
- «день 1 ок» — тогда тикаем progress и идём в день 2  
- «не понял вот эту строку в App.tsx:45» — разберём одну строку

---

## Сдано, если

Без шпаргалки отвечаешь:

1. Почему мутация массива из `useState` не обновляет UI.  
2. Как ребёнок говорит родителю «добавили id» без return и без стора.  
3. Чем `onClick={fn(id)}` отличается от `onClick={() => fn(id)}`.
