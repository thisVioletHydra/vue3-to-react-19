// PUBLIC API слайса. Снаружи импортируй только отсюда:
//   import { AuthLayout } from "@/app/layouts/auth-layout"
// (алиас @ потом сам, пока относительные пути)
//
// EXPORT: named (не default) — layout редко в React.lazy, default не нужен.

export { AuthLayout } from "./ui/AuthLayout";
