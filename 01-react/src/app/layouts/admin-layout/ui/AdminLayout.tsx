// FSD: app/layouts/admin-layout — рамка админки (сайдбар + Outlet).
// EXPORT: named. Default не нужен.

export function AdminLayout() {
  return (
    <main>
      <p className="box1">ti v adminke komrad</p>
    </main>
  );
}
